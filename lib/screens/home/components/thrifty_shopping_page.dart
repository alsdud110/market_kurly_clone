import 'package:flutter/material.dart';

class ThriftyShoppingPage extends StatelessWidget {
  const ThriftyShoppingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("ThriftyShoppingPage"),
    );
  }
}
